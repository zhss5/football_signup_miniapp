<view class="page">
  <view class="panel">
    <text class="title">{{formTitleText}}</text>
    <text class="hint">{{formHintText}}</text>

    <view class="profile-row">
      <button class="avatar-button" open-type="chooseAvatar" bindchooseavatar="onChooseAvatar">
        <image wx:if="{{avatarUrl}}" class="avatar-image" src="{{avatarUrl}}" mode="aspectFill" />
        <view wx:else class="avatar-placeholder"></view>
      </button>
      <view class="profile-copy">
        <text class="label">{{i18n.activityJoin.avatar}}</text>
        <text class="hint small-hint">{{i18n.activityJoin.avatarHint}}</text>
        <button
          wx:if="{{isEditMode && avatarUrl}}"
          class="avatar-remove-button"
          bindtap="onClearAvatar"
        >{{i18n.activityJoin.removeAvatar}}</button>
      </view>
    </view>

    <view class="field">
      <text class="label">{{i18n.activityJoin.signupName}}</text>
      <input
        class="input"
        type="nickname"
        placeholder="{{i18n.activityJoin.signupNamePlaceholder}}"
        value="{{signupName}}"
        bindinput="onNameInput"
      />
    </view>

    <view class="field">
      <text class="label">{{i18n.activityJoin.preferredPositions}}</text>
      <text class="hint small-hint">{{i18n.activityJoin.preferredPositionsHint}}</text>
      <view class="position-grid">
        <button
          wx:for="{{positionOptions}}"
          wx:key="value"
          class="position-option {{item.selected ? 'position-option-selected' : ''}}"
          data-value="{{item.value}}"
          bindtap="onPositionTap"
        >{{item.label}}</button>
      </view>
    </view>

    <button class="confirm-button" bindtap="onSubmit" loading="{{submitting}}">{{confirmButtonText}}</button>
  </view>
</view>
